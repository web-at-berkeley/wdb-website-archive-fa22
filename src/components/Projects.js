import React from 'react';

import SiteNavbar from './SiteNavbar';

const Projects = () => {
	return (
		<div className="team">
			<SiteNavbar />
			<h1>Projects</h1>
		</div>
	);
};

export default Projects;
